# ================================
# COMMON APPLICATION CONFIGURATION
# ================================

# ? Set active profile (Make sure 'application-dev.properties' or 'application-prod.properties' exists)
spring.profiles.active=dev

# ?? Hibernate (JPA) settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ?? JWT Token expiration (1 day = 86400000 ms)
spring.security.jwt.expiration=86400000

# ? Email configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ruthvikmt001@gmail.com
spring.mail.password=dcxeyarysvemjrjp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.jpa.hibernate.ddl-auto=update

